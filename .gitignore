# Dependencies (never commit — run npm install locally to regenerate)
node_modules/

# Brochure PDF build artifacts
brochure/node_modules/
brochure/All-Pro-Building-Supplies-Brochure-temp.pdf
brochure/sell-sheets/html/

# Cloudflare Worker local cache
backend/.wrangler/

# Cloudflare Worker local dev secrets (never commit)
backend/.dev.vars

# Cloud Agent environment is the existing personal dashboard env.
# Committing this file made Cursor spin up a second repo/environment.
.cursor/environment.json

# OS / editor
.DS_Store
Thumbs.db
*.log
__pycache__/
*.pyc
